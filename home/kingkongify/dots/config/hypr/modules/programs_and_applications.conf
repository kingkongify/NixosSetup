# ----------------------------------------------------------------
# Programs & Applications
# ----------------------------------------------------------------

$terminal = kitty
$fileManager = thunar
$menu = rofi -show drun
$browser = firefox
$editor = vscodium


# ----------------------------------------------------------------
# Autostart
# ----------------------------------------------------------------

exec-once = systemctl --user start hyprpolkitagent
exec-once = nm-applet &
exec-once = dunst &
exec-once = eww daemon && eww open-many bar activate-linux

# Wallpaper manager
exec-once = sleep 1 && /home/kingkongify/.local/bin/livewallpaper.sh -l
exec-once = swww-daemon

# Optional utilities
exec-once = cliphist wipe & wl-paste --watch cliphist store &
exec-once = notify-send "dont forget yo goals blud"
# exec-once = copyq &                                                  # Clipboard UI
# exec-once = wlsunset -l 40.71 -L -74.00 -t 4500 -T 6500 &           # Night light / blue light filter
# exec-once = ~/.config/hypr/scripts/startup.sh                       # Custom startup script
